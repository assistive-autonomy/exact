# Default data generation configuration
num_train_samples: 500
num_val_samples: 100
num_steps: 100  # Number of timesteps per motion sequence

# Program generation parameters
program_generation:
  min_units: 1
  max_units: 3
  min_value: 0.1
  max_value: 1.0
